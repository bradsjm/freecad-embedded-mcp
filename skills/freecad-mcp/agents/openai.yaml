interface:
  display_name: "FreeCAD MCP"
  short_description: "Create, validate, and export FreeCAD models through MCP"
  default_prompt: "Use $freecad-mcp to create or refine a model in FreeCAD, validate the geometry, and export the result."
